// eslint-disable-next-line
function Box({ heading, value }) {
  return (
    <div className="">
      <span className="block text-[14px] text-white">{heading}</span>
      <span className="text[14px] text-white ">{value}</span>
    </div>
  );
}

export default Box;
